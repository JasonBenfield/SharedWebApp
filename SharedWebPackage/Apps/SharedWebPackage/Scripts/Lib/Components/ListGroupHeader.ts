import { BasicListGroupItemView } from "../Views/ListGroup";
import { BasicComponent } from "./BasicComponent";

export class ListGroupHeader extends BasicComponent {
    constructor(view: BasicListGroupItemView) {
        super(view);
    }
}